<% db = p("db") %>
<% if_p("#{db}.roles") do |roles| %>
local   all             all                                     trust
host    all             all             127.0.0.1/32            trust
host    all             all             ::1/128                 trust
host    all             all             0.0.0.0/0               md5
<% roles.each do |role| %>
host    all  <%= role.name %> 0.0.0.0/0 md5
<% end %>
