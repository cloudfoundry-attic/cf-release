#!/bin/bash

nc -z 127.0.0.1 <%= p('hm9000.port') %>

# http://www.consul.io/docs/agent/checks.html
if [ $? -ne 0 ]; then
    exit 2 # Exit higher than 1 to ensure serivce is registered as critical
fi
