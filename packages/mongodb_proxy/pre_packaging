#abort script on any command that exit with a non zero value
set -e

cd ${BUILD_DIR}

cd services/tools/mongodb_proxy/src/

tar zxf golang/goyaml.src.tar.gz

tar zxf golang/log4go.src.tar.gz

tar zxf golang/mgo.src.tar.gz
