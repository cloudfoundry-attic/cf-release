# abort script on any command that exit with a non zero value
set -e

cd ${BUILD_DIR}

# duh, this is what happens when warden and friends live together
TMPDIR=$(mktemp -d ./tmp.XXX)
mv warden $TMPDIR
mv $TMPDIR/warden/warden .
rm -rf $TMPDIR

pkg_utils/vendor.sh warden

rm -rf pkg_utils
